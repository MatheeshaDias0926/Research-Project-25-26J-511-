opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
requests>=2.31.0
pygame>=2.5.0
gpiozero>=2.0.0
ultralytics>=8.0.0
picamera2
# ── Face recognition (required by verifier.py / LocalFaceVerifier) ──
# dlib must be installed first (requires cmake + a C++ compiler on the Pi)
# On Raspberry Pi: sudo apt-get install cmake build-essential libopenblas-dev
# then: pip install dlib face_recognition
dlib>=19.24.0
face_recognition>=1.3.0
