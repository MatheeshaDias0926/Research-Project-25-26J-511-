opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
requests>=2.31.0
pygame>=2.5.0
gpiozero>=2.0.0
ultralytics>=8.0.0
picamera2
# ── Face verification is now REMOTE (Backend → ML Service) ──
# dlib and face_recognition are NO LONGER needed on the Pi.
# They remain installed on the ML server only.
